%

*P3: Compute the sum of natural numbers up to a given number.

let int n,i,sum=0;

write("Give me the n:");
read(n);
if n< 0 then {
   write("Enter a positive number");
}
else
{   sum = 0;
    while n > 0 do
    {
       sum += num;
       n -= 1;
   }
   write(sum);
}
%